
def node(self, next, data):
    self.next = None
    self.data = None

def linked_list(self, head):
    self.head = None


# initialze a variable to keep track of the most recent node value will be null

# initalze a variable to track to track the current head of the input list

# while there are still nodes to reverse

    # initialze a variable to hold the next node of the current head
    # assign the current head next property to the previous node
    # set the previous node to the current head
    # set the current head to the next node that was tracked at the beginning of the loop

# end while
# Set the inputs lists head to the previous nodes
# return reversed list
